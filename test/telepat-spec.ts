import {  } from "../src";
import * as chai from "chai";
import {} from "mocha";

const expect = chai.expect;

describe("class", () => {
  it("should greet with message", () => {
    expect("aaa").to.equal("Bonjour, friend!");
  });
});
